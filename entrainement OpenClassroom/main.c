#include <string.h>
void tokenisation(char prompt[],char **vocabulaire,char **token ,int T) {
    char tokens[10];
    char N = strlen(*vocabulaire);
    int identifie =0;
    while (identifie < T) {
        for (int i = 0; i; i++) {
            if (strncmp(prompt,*));
        }
    }
}